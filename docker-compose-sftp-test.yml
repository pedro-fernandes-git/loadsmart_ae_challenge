version: '3.8'

services:
  sftp-test:
    image: atmoz/sftp:latest
    container_name: loadsmart-sftp-test
    ports:
      - "2223:22"
    # Remove the volumes section entirely for testing
    command: testuser:testpass:1001:1001:upload
    restart: unless-stopped
